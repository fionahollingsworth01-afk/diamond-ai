{
  "name": "diamond-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run validate:data && vite",
    "validate:data": "node scripts/validateFiveOaksData.mjs",
    "build": "npm run validate:data && vite build",
    "test": "npm run validate:data && node tests/fiveOaksSearch.test.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
